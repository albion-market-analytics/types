<%
 const itemNames = '\'' + items.map(item => item.UniqueName).join("' | '") + '\'';
 const _regions = world.map(region => {
     return `    "${region.Index}": "${region.UniqueName}",`;
 }).join('\n');
 %>

export type City = 'Thetford' | 'Fort Sterling' | 'Lymhurst' | 'Bridgewatch' | 'Martlock' | 'Caerleon';
export type ItemName = <%- itemNames %>


export type RegionList = {
<%- _regions %>
};

export type Region = keyof RegionList;

export type Quality = 1 | 2 | 3 | 4 | 5;
